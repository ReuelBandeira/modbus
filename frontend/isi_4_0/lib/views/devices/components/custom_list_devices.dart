import 'package:flutter/material.dart';

class CustomListDevices extends StatelessWidget {
  const CustomListDevices({super.key, required this.devices});
  final List<dynamic> devices;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
